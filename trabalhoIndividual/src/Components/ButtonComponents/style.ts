import { StyleSheet } from "react-native";

export const batatinha = StyleSheet.create({
  button: {
    backgroundColor: '#000',
    width: '90%',
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 10,
    height: 50,
  },

  textButton: {
    fontSize: 35,
    color: "white",
    fontWeight: '800'
  }
});